#ifndef GET_HPP
#define GET_HPP

#include <includes.h>
#include <request.h>
#include <response.h>

void root (Request& req, Response& res);
void index_html (Request& req, Response& res);

#endif // GET_HPP